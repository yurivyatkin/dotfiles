[user]
  name = Michael Gershkovich
  email = mgershko@users.noreply.github.com
  username = mgershko
[core]
  editor = vim
  whitespace = off
  excludesfile = ~/.gitignore_global
[color]
  ui = true
[push]
  default = current
[alias]
  bak = !git branch `git br | grep '*' | sed -e 's/* /BAK_/'`
  br = branch
  ci = commit
  co = checkout
  st = status
  ignore = update-index --assume-unchanged
  ignored = !git ls-files -v | grep "^[[:lower:]]"
  unignore = update-index --no-assume-unchanged
